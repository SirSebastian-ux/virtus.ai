"use client";

export default function DecisionCenterPage() {
  return (
    <main className="min-h-screen bg-slate-950 px-4 py-10 text-white">
      <div className="mx-auto max-w-6xl rounded-3xl border border-white/10 bg-white/10 p-8">
        <p className="text-sm font-semibold uppercase tracking-[0.3em] text-cyan-200">
          Decision Center
        </p>
        <h1 className="mt-4 text-4xl font-bold">Executive Decision Center</h1>
        <p className="mt-4 max-w-3xl text-slate-300">
          Temporary safe page restored. The backend Decision Center API is built; next we will add the full UI again cleanly.
        </p>
      </div>
    </main>
  );
}
